# Contributing

Use Node 22 or later. Keep changes portable across macOS, Linux, and Windows, add deterministic tests for platform behavior, and run `npm run check` and `npm test` before proposing a change. Do not add home-directory assumptions, credentials, or generated local state to the package.
